
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive rules for development: Allow read/write access to any
    // authenticated user. This is NOT secure for production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

    